"use client";

/**
 * BottomNav supprimé — l'app est plus simple sans barre de navigation fixe en bas.
 * Le header + hamburger suffit pour la navigation.
 */
export function BottomNav() {
  return null;
}
